{
    "name": "pokeclicker",
    "version": "1.1.1",
    "scripts": {
        "dev": "npm run clean && npm run pretty && npm run build && npm run sed",
        "build": "tsc --build",
        "clean": "tsc --build --clean",
        "pretty": "prettier --write .",
        "sed": "sed -i \"/^import/d\" dist/*.js"
    },
    "license": "GNU3",
    "devDependencies": {
        "prettier": "2.7.1",
        "typescript": "^4.8.4"
    }
}
